# AI Profile Documentation

This repository hosts the documentation site for AI Profile, built with [Docusaurus](https://docusaurus.io/).

## Structure
- `website/`: Docusaurus project source.
- Content under `website/docs/`.

## Deployment
Deployed to GitHub Pages via `gh-pages` branch.

Build & deploy:
```bash
cd website
npm install
npm run deploy
```
