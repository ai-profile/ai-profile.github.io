{
  "name": "ai-profile-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "dev": "cd notes && npm run dev",
    "build": "cd notes && npm run build && cd .. && rm -rf dist && mkdir -p dist/notes && cp index.html dist/ && cp -r notes/build/* dist/notes/"
  }
}
