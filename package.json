{
  "name": "ai-profile-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "dev": "cd docusaurus_project && npm run dev",
    "build": "cd docusaurus_project && npm run build && cd .. && rm -rf dist && mkdir -p dist/notes && cp index.html dist/ && cp -r docusaurus_project/build/* dist/notes/"
  }
}
